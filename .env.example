# Copy to .env and fill in. Never commit .env.
GEMINI_API_KEY=
# Optional. The extractor already walks a chain of current models and falls
# through on failure, so leave this unset unless you want to pin one.
# TIEOUT_MODEL=
TIEOUT_MAX_ADJUDICATIONS=40
