PyMuPDF>=1.24
fastapi>=0.110
uvicorn[standard]>=0.27
google-genai>=0.3
python-dotenv>=1.0
pyyaml>=6.0
python-multipart>=0.0.9
pytest>=8.0
# fastapi.testclient needs httpx. It arrives transitively via google-genai
# today; declared here so the test suite does not depend on that accident.
httpx>=0.27
